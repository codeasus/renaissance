<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:background="@drawable/rv_item_bg_meal"
    android:padding="6dp">

    <ImageView
        android:id="@+id/iv_cocktail"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:contentDescription="@string/image_description_meal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tv_cocktail_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="12dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/purple_500"
        app:layout_constraintBottom_toBottomOf="@id/iv_cocktail"
        app:layout_constraintStart_toEndOf="@id/iv_cocktail"
        app:layout_constraintTop_toTopOf="@id/iv_cocktail" />

</androidx.constraintlayout.widget.ConstraintLayout>